---
/**
 * Weather Page - Client-side rendered
 * Reads location from URL params in the browser
 */
import BaseLayout from "../layouts/BaseLayout.astro";
import WeatherPage from "../islands/WeatherPage";
---

<BaseLayout title="Weather - ClimaWiki">
  <WeatherPage client:only="preact" />
</BaseLayout>
